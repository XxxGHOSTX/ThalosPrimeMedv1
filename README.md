# Thalos Prime v1

Thalos Prime v1 — a minimal Node + TypeScript starter scaffold (pnpm).

Getting started

1. Install dependencies: `pnpm install`
2. Start dev server: `pnpm run dev`
3. Build: `pnpm run build`
4. Run: `pnpm start`

See LICENSE for terms.